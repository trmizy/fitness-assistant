import { PhasePlaceholder } from "../../../src/components/navigation/PhasePlaceholder";

/** SH-13 — thư viện thực phẩm. Part of the nutrition domain, built in Phase 6; the hub card
 * links here so the four-destination layout from the reference is complete today. */
export default function FoodLibraryScreen() {
  return (
    <PhasePlaceholder
      title="Thực phẩm"
      phase="Phase 6"
      description="Tra cứu calo/protein/carb/fat theo 100g, lọc theo nhóm thực phẩm."
    />
  );
}
