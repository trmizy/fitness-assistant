# Để trống = tự nhận diện IP LAN (dùng khi test qua Expo Go trên điện thoại thật,
#   cùng WiFi với máy dev) — logic tự detect nằm ở app.config.ts + scripts/detect-api-url.js
# Điền http://localhost:3000 = ép chạy chế độ web/laptop only (npm run start:web)
EXPO_PUBLIC_API_URL=

EXPO_PUBLIC_MOCK=0
