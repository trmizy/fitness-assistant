FROM node:20-alpine
RUN corepack enable && corepack prepare pnpm@8.15.0 --activate
RUN apk add --no-cache openssl wget

WORKDIR /app

# ── Install dependencies (cached layer) ──────────────────────────────────────
COPY pnpm-workspace.yaml package.json pnpm-lock.yaml ./
COPY backend/shared/package.json                       ./backend/shared/
COPY backend/services/gym-service/package.json         ./backend/services/gym-service/
RUN pnpm install --filter @gym-coach/gym-service

# ── Pre-build @gym-coach/shared ───────────────────────────────────────────────
COPY tsconfig.base.json                                ./
COPY backend/shared/tsconfig.json                      ./backend/shared/
COPY backend/shared/src                                ./backend/shared/src
RUN pnpm --filter @gym-coach/shared build

# ── Generate Prisma client ────────────────────────────────────────────────────
COPY backend/services/gym-service/prisma               ./backend/services/gym-service/prisma
RUN pnpm --filter @gym-coach/gym-service db:generate

# ── Copy service config (src/ is bind-mounted at runtime) ─────────────────────
COPY backend/services/gym-service/tsconfig.json        ./backend/services/gym-service/

WORKDIR /app/backend/services/gym-service
EXPOSE 3006

CMD ["sh", "-c", "node_modules/.bin/prisma migrate deploy && pnpm run dev"]
